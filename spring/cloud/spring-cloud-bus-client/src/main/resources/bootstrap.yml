spring:
  application:
    name: spring-cloud-bus-client
  profiles:
    active: default  
  cloud:
    config:
      # fail-fast: true
      uri: http://localhost:8081
      label: master
      
management:
  endpoints:
    web:
      exposure:
        include: "*"